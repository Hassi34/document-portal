############################################################
# Backup Service Environment Example (.env) – YAML First    #
############################################################

# Most configuration now lives in backup_config.yaml.
# Only set env vars here for overrides, local testing, or logging.

# -----------------------------------------------------------------
# AWS Credentials (LOCAL DEV ONLY) - use IAM task role in ECS/Fargate
# -----------------------------------------------------------------
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
AWS_REGION="us-east-2"

# Path to configuration file (if not using default ./backup_config.yaml)
# BACKUP_CONFIG_PATH="/app/backup_config.yaml"

# Manifest filename (kept outside YAML so you can A/B test quickly)
BACKUP_MANIFEST=".backup_manifest.json"

# Logging level (INFO|DEBUG|WARNING|ERROR)
LOG_LEVEL="INFO"

# Optional feature toggles (normally let CLI/YAML decide)
# BACKUP_ARCHIVE="0"          # Set to 1 to force archive mode without CLI flag
# BACKUP_INCREMENTAL="1"       # Set 0 to disable incremental (per-file) when not archiving

# Future: lock file path placeholder (not implemented)
# BACKUP_LOCK_FILE="/tmp/backup.lock"

# Everything else (bucket, prefix, include_dirs, interval) is taken
# from backup_config.yaml unless a CLI flag override is provided.
